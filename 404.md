---
layout: page
title: Page Not Found (๑·̀ᗝ·́)૭
permalink: /404.html
---

<script>
(function() {
  var path = window.location.pathname;
  // Match /guides/<name> where <name> is a single segment (not already a module path)
  var match = path.match(/^\/guides\/([^\/]+)$/);
  if (match) {
    var guideName = match[1];
    // Only redirect if it doesn't look like a module path (m1_1, m2_3, etc.)
    if (!guideName.match(/^m\d+_\d+$/)) {
      window.location.href = '/guides/' + guideName + '/m1_1';
    }
  }
})();
</script>

<div style="display: flex; align-items: center; flex-wrap: wrap;">

  <a href="{{ site.baseurl }}/">
    <img src="{{ site.baseurl }}/images/404.png" alt="Ren" style="margin-right: 60px; width: 200px; flex-shrink: 0; box-shadow: none;" />
  </a>

  <div>
    <h3>It's a 404 Error!</h3>
    <p>Sorry, but the page you're looking for is in another castle.</p>
    <p>
        <a class="btn" href="{{ site.baseurl }}/">
            <i class="fa fa-home" aria-hidden="true"></i> Return to the homepage
        </a>
    </p>
  </div>

</div>
